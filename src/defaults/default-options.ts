import { Options } from '../types';

export const defaultOptions: Options = {
  minCount: -1,
  maxCount: -1,
  minSize: -1,
  maxSize: -1,
  allowedExtensions: [],
  disallowedExtensions: [],
  allowedMimetypes: [],
  disallowedMimetypes: [],
  abortEarly: true,
};
